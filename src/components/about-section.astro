---
interface Props {
  class?: string;
  title: string;
}

const { class: className, title } = Astro.props;
---

<section class={className}>
  <h2
    class="mb-4 font-mono font-medium text-muted-foreground text-xs uppercase"
  >
    {title}
  </h2>
  <slot />
</section>
